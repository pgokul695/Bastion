<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.bastion" path="/org/gnome/shell/extensions/bastion/">
    
    <key name="current-provider" type="s">
      <default>'Google'</default>
      <summary>Selected DNS Provider</summary>
    </key>

    <key name="custom-dns" type="(sss)">
      <default>('Custom', '10.0.0.1', '10.0.0.2')</default>
      <summary>Custom DNS Configuration</summary>
    </key>

    <key name="enable-encrypted-dns" type="b">
      <default>false</default>
      <summary>Enable DNS over TLS (DoT) in Secure Mode</summary>
    </key>

    <key name="strict-dns-mode" type="b">
      <default>false</default>
      <summary>Enable Strict DNS (No Fallback)</summary>
      <description>If true, connection fails if DoT fails (yes). If false, falls back to clear text (opportunistic).</description>
    </key>

    <key name="enable-encrypted-dns-login" type="b">
      <default>false</default>
      <summary>Enable DNS over TLS (DoT) in Login Mode</summary>
    </key>

    <key name="toggle-shortcut" type="as">
      <default>['&lt;Super&gt;b']</default>
      <summary>Keyboard shortcut to toggle Bastion mode</summary>
    </key>

  </schema>
</schemalist>
